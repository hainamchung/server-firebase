{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "start-app",
            "type": "shell",
            "command": "lsof -ti:5000 | xargs kill -9 2>/dev/null || true && sleep 1 && export WS_PORT=5000 && npm run start",
            "runOptions": {
                "runOn": "folderOpen"
            },
        }
    ]
}
